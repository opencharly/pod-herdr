# 2026.251.0850 — chore(migrate): manifest schema 2026.240.1943 → 2026.249.2125 (required by the advanced charly pin)

## Summary

Schema-stamp migration for the manifest (`charly.yml`): `version: 2026.240.1943` →
**`2026.249.2125`** — required by the charly main this repo now pins (the advanced
charly validates the manifest at the HEAD schema stamp).

**This PR's diff vs base carries TWO items** (the branch was cut from the pre-sync
base):
1. the `charly` submodule bump `afb1c5a9` → **`bb9bfc91`** (main, merged — the
   android-wave cachyos-box fixes). The value is identical to what the previous PR
   (#7 / #5) already merged onto main, so on main this line is a no-op re-record;
2. the `charly.yml` schema stamp `2026.240.1943` → `2026.249.2125` (chore(migrate)
   commit — the NET-new change on main).

Migration applied with the sanctioned tool: `charly migrate` (idempotent, the
declarative migration table — `migrated to schema 2026.249.2125`).

## How tested

```
cd <repo> && charly migrate   => "migrated to schema 2026.249.2125"
charly box validate          => OK — 0 warnings, 0 errors
```

The repo's `build` CI job (which failed on main with `schema 2026.249.2125 is
required (found 2026.240.1943). Run: charly migrate`) now passes the validate step
at the pinned charly — **build job: PASS** on this PR's head.

## Rulebook compliance

- **R1**: the failed build job root-caused to the schema-stamp requirement of the
  advanced charly main — fixed with the sanctioned migration tool.
- **R3**: no duplication — the declarative migration table owns the stamp.
- **R4**: no workarounds — `charly migrate`, not a hand bump.
- **R6**: git safety — feat/ branch only, no force-push, no direct main push.
- **R10**: validated at the pinned charly (the repo's own CI runs the same gate).

## Change classification

- **Change class**: chore (schema-stamp migration + submodule pin sync — the pin
  value already on main via the prior merge)
- **Verification gate**: `charly migrate` + `charly box validate` output + the
  PASSing `build` CI job above
- **Attribution tier**: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (95% confidence)*