# 2026.245.0933 — stack: herdr box + candy + check-herdr-pod R10 bed (external plugin, fully green)

## Summary

The herdr stack (herdr.dev terminal multiplexer) for opencharly, as an
external-candy box + disposable R10 bed:

- **candy/herdr** — pinned herdr v0.8.2 release binary, supervised headless
  server (`HERDR_SESSION=base`), and a socat TCP:8095 bridge to the server's
  NDJSON unix socket (session-aware socket path — found + fixed in-bed).
- **box/herdr** — fedora:43 image composing herdr + socat + supervisord +
  the plugin-herdr EXTERNAL candy (pinned at the merged tag
  `v2026.245.0926` — pin discipline; zero charly-module changes). ADE:
  deterministic build-context plan (binary + scripts + pinned version).
- **check-herdr-pod** — disposable R10 bed probing BOTH surfaces of the
  plugin-herdr plugin live: the `herdr:` check verb (host-side over the
  reverse channel) AND the `charly herdr` CLI over the published port
  (R3 parity) — plus the full workspace/split/run/wait/agent flow.
- **herdr-box skill entity** for the marketplace corpus.
- **Repo scaffolding** — `.github/workflows/` (the org-standard trio:
  `pr-validator.yml`, `tag-on-merge.yml`, `deploy.yml` — installed in EVERY
  opencharly repo, self-install per the org ground rule 3 the RP1 validator
  already accepted; their write permissions + `secrets: inherit` are the
  org-standard dispatcher surface, nothing repo-specific), `.gitmodules`
  (charly submodule pin, merged main HEAD), `.gitignore`, `README.md`.

### Files in this PR (12)

`candy/herdr/charly.yml`, `candy/herdr/start-herdr.sh`,
`candy/herdr/start-bridge.sh`, `box/herdr/charly.yml`, `charly.yml`
(bed + skill entity + defaults), `.github/workflows/{pr-validator,tag-on-merge,deploy}.yml`,
`.gitmodules`, `.gitignore`, `README.md`.

## How tested

Pasted output — validation + the full R10 bed run:

```
$ charly box validate
charly box validate: OK — checked 7 candies, 1 box, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors
```

```
$ charly check run check-herdr-pod
[image-build] PASS  [check-image] PASS  [deploy-add] PASS  [config] PASS
[start] PASS  [bring-up-members] PASS  [check-live] PASS
[feature-run] PASS  [update] PASS  [check-live-rebuild] PASS
[feature-run-rebuild] PASS  [cleanup] PASS  [cleanup-members] PASS
charly check run check-herdr-pod: PASS (steps=13)
```

The herdr-specific live steps (on the deployed pod):

```
PASS [herdr-pod-verb-ping]           pong (protocol 20, version 0.8.2)
PASS [herdr-pod-cli-status]          exit=0
PASS [herdr-pod-cli-flow]            exit=0   # create → split → run → wait-output → agent report
PASS [herdr-pod-verb-workspace-list] * w1  herdr-bed  tabs=1 panes=2  working
PASS [herdr-pod-verb-agent-list]     * bed-agent  working  pane=w1:p2 workspace=w1 /home/user
PASS [herdr-pod-verb-pane-wait]      output matched in w1:p2: sh-5.3$ echo herdr-bed-marker-42
PASS [herdr-pod-verb-snapshot]       pong (protocol 20, version 0.8.2)
```

## Rulebook compliance

- **R1** — the socket-path bug was RCA'd from live bed failures + fixed.
- **R3** — one surface per concern; no duplicated client logic.
- **R4** — no workarounds; the bridge waits on the real session-aware path.
- **R6/R7** — branch-per-change, no force-push; gate proven by pasted live run.
- **R10** — `charly check run check-herdr-pod` fully green on a disposable pod
  (fresh + update + rebuild), pasted above.
- **Pin discipline** — plugin-herdr pinned at the merged tag (never a branch
  ref in the committed tree); charly submodule at merged main HEAD.
- **ADE** — box node carries a deterministic `plan:` (binary + scripts +
  version checks); candy + deploy nodes carry theirs.
- **PR-only landing** — no direct push to main.

## Change classification

- Change class: feature (herdr stack box + candy + R10 bed + skill entity)
- Verification gate: `charly check run check-herdr-pod` FULLY GREEN (13/13) + box validate
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*