# 2026.245.0944 — skill: herdr-box family automation (marketplace families map has no 'herdr' family)

## Summary

The `herdr-box` skill entity declared family `herdr`, which is not in the
marketplace `families:` map (owned by pod-charly-hooks) — the corpus
regeneration rejects unknown families. The skill now uses the existing
`automation` family, sibling of the plugin-herdr `herdr` CLI skill.

## How tested

```
$ charly box validate
charly box validate: OK — checked 7 candies, 1 box, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors
```

(The corpus regeneration consuming this repo's skill entity runs on the
marketplace side after this lands a tag — the drift gate there.)

## Rulebook compliance

- **R1** — the family mismatch was surfaced by the marketplace generator
  ("skill herdr-box declares family herdr which the marketplace entity does
  not declare"), fixed by reusing the declared automation family.
- **R3** — one family declaration; no new parallel map copy in this repo.

## Change classification

- Change class: fix (skill metadata)
- Verification gate: `box validate`
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*