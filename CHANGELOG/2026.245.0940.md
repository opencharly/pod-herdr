# 2026.245.0940 — fix: remove the plugin-module step from the candy gate (copy error — herdr plugin is an external candy ref)

## Summary

The pod-herdr candy gate (deploy.yml) carried a step copied from the plugin
repo's gate ("The plugin module builds and its tests pass" — `cd candy/plugin-herdr && go build…`). pod-herdr has NO local plugin module (the herdr plugin is an EXTERNAL candy ref), so the step failed on every PR — and PR #1 merged on a head that still had it. This removes the misplaced step, returning the gate to the org-standard candy check (build the pinned charly + `box validate`).

## How tested

The step is 3 deleted lines; the remaining gate steps were green on the
previous head:

```
$ charly box validate
charly box validate: OK — checked 7 candies, 1 box, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors
```

Full R10 bed for the stack remains: `charly check run check-herdr-pod` PASS (13/13 steps) — pasted in PR #1.

## Rulebook compliance

- **R1** — the failing step was RCA'd to a copy error (the plugin-repo gate template), removed.
- **R3** — the org-standard candy gate is the single source; no repo-specific step.
- **PR-only landing** — branch-per-change.

## Change classification

- Change class: fix (CI gate)
- Verification gate: `box validate` (pasted) — the gate's own run on this head
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*